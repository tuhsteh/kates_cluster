---
# Minimal Environment
# Base system setup only
# Usage: ansible-playbook minimal.yaml -i hosts.inv
# Resources: Minimal
# Use this as a foundation before adding other workload-specific playbooks

# Compose the minimal profile from shared playbooks.
- name: Import shared minimal baseline
  import_playbook: playbooks/shared/minimal_base.yaml
