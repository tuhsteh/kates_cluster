### mdns-avahi-rewrite: Rewrite mdns role — Avahi + /etc/hosts for Trixie
- **Avoid**: Three template/fact bugs in the original stub: (1) `slurp` returns base64 — comparing raw output to `'trixie'` always false; use `ansible_distribution_release` fact instead. (2) `hostvars[host]['ansible_host']` used as IP without confirming inventory defines it — use `ansible_default_ipv4.address` with `| default()` fallback. (3) Template appended `.local` to inventory hostnames already ending in `.local` → `kate0.local.local`. Inspect the hostname suffix before appending domain labels.
- **Repeat**: Researcher + explore dispatched in parallel — full picture (Trixie TC decision + existing role state) in one round-trip, no sequential wait. `allow-interfaces=eth0` in avahi-daemon.conf is a portable k3s safety pattern: any role installing multicast-aware services must restrict interfaces before k3s flannel/CNI creates virtual NICs.
- **Updated**: `.context/domains/nanopc-t4-hardware.md` (added mDNS/Hostname Resolution section + mdns row in Role Impact Matrix), `.context/domains/ansible-playbook.md` (added mdns to linux tag row), `.context/standards/ansible-gotchas.md` (created — slurp/base64, ansible_host vs gathered fact, hostname suffix patterns).

### friendlyelec-linux-review: FriendlyElec Debian 13 linux role corrections
- **Avoid**: Domain documents written for a specific OS must declare their OS scope at the top. `nanopc-t4-hardware.md` described Armbian conventions without noting the Armbian assumption — when the boards moved to FriendlyElec, every armbianEnv.txt reference became silently wrong and the ssd_mount role would have formatted the NVMe root filesystem on the first run. Fix: add an OS-scope note to hardware domain documents (e.g., "This document covers Armbian Bookworm — for FriendlyElec images see X").
- **Repeat**: Before writing or updating Ansible roles for a new OS/distro, run a six-command hardware verification checklist on an actual device: `ls /boot/extlinux/extlinux.conf`, `cat /proc/cmdline`, `mount | grep overlay`, `swapon --show`, `which update-initramfs`, `systemctl status zramswap`. In under a minute this revealed three blocking facts: no extlinux.conf (cmdline not Ansible-manageable), NVMe IS the root (ssd_mount would have destroyed the filesystem), and no swap whatsoever. No amount of research substitutes for these six commands on real hardware.
- **Updated**: `domains/nanopc-t4-hardware.md` — replaced Armbian-specific sections with FriendlyElec Debian 13 facts: boot scheme (raw Rockchip partitions, no extlinux.conf), eMMC device node (mmcblk2), NVMe-as-root layout, kernel 4.19.232, no swap, cgroup_enable=cpuset gap, updated role impact matrix.

### log-ramdisk: Mount /var/log as tmpfs ramdisk
- **Avoid**: Each `bash` tool call runs in a fresh shell — `git checkout -b feature/log-ramdisk` did not persist to a later commit call, so the commit landed on `main`. Fix: always include `git checkout <branch>` in the same bash call as the git operation (e.g., `git checkout feature/log-ramdisk && git commit ...`), or verify branch with `git branch` immediately before committing.
- **Repeat**: @reviewer caught a silent correctness risk (journald switching to persistent mode on the tmpfs, filling the ramdisk unexpectedly) that wasn't obvious from the implementation alone. Worth the review pass even on small new roles.
- **Updated**: nothing to promote — no committed `.context/` in this project (globally gitignored). If `initialize-repo` is run in future, promote the journald/tmpfs pattern to `domains/raspberry-pi.md`.

### k3s-ssd-data-dir: k3s data-dir and kubelet root-dir on SSD with systemd mount ordering
- **Avoid**: When relocating a service's data directory, grep the entire role for **all** hardcoded references to the old default path before committing. The coder added new tasks and templates but did not audit existing tasks — token path (`/var/lib/rancher/k3s/server/token`) was hardcoded in 3 places and would have caused `wait_for` to time out on the first full run. Fix: always `grep -r <old-default-path> roles/<role>/` after any data-dir config change.
- **Repeat**: Extract a single role default variable (`k3s_data_dir`) and use it everywhere in tasks, templates, and handlers. This keeps the path DRY and makes the class of drift-bug structurally impossible. The reviewer suggested it; the fix was straightforward because all references were in one role.
- **Updated**: Appended k3s path inventory, `/var/lib/kubelet` exclusion explanation, systemd drop-in pattern, and SSD mount guard to `.context/domains/raspberry-pi-hardware.md`.

### ssd-fstab-mount: UUID-based m.2 SSD discovery and persistent mount
- **Avoid**: `ansible.builtin.command` runs unconditionally in `--check` mode. When it follows a `community.general.filesystem` task (which correctly skips in check mode), it operates on an unformatted device, returns empty output, and triggers any UUID-empty fail guard — producing a misleading fatal on dry-runs of unprovisioned nodes. Fix: gate the command and its dependents with `when: not ansible_check_mode`; add a placeholder set_fact for the check-mode path. Promoted to `.context/domains/raspberry-pi-hardware.md`.
- **Repeat**: Researcher-first for hardware-specific discovery paid off — surfaced the CM4 `rotational: "0"` gotcha (both SD cards and SSDs report non-rotational) before any code was written, avoiding a hard-to-debug silent mis-identification. Worth dispatching @researcher whenever Ansible meets unfamiliar hardware.
- **Updated**: Created `.context/domains/raspberry-pi-hardware.md` with CM4 device naming, NVMe-first discovery pattern, check-mode safety pattern, and `ansible.posix.mount` fstab gotchas.

### longhorn-storage: Longhorn v1.11.1 distributed storage via k3s HelmChart CRD
- **Avoid**: Re-declaring a variable owned by another role as a local `defaults/main.yaml` entry. `longhorn/defaults/main.yaml` originally duplicated `k3s_leader_data_dir` from `k3s_leader`. When both roles run in the same play the variable is already in scope — duplicating the default creates a silent drift hazard: if the owning role's value ever changes, the consumer silently uses the stale value with no error or warning. Fix: use the variable directly in tasks and add a `# Depends on: <role> — provides <variable>` comment above the first task that uses it.
- **Repeat**: Researcher-first before coding any new infrastructure tool (third task in a row this has paid off). Surfaced `dmsetup` (hard-required Longhorn package, absent from most community install guides) and the `iscsid.socket` vs `iscsid.service` Bookworm socket-activation distinction before any code was written — both would have caused silent runtime failures. Also: parallel dispatch of two independent coder agents (longhorn_prereqs + longhorn) halved wall-clock time with no coordination overhead.
- **Updated**: Appended Longhorn section to `.context/domains/raspberry-pi-hardware.md` covering required packages (including dmsetup), iscsid.socket socket-activation pattern, multipathd mitigation, k3s HelmChart CRD with failurePolicy:abort, and known ARM64/k3s gotchas. Cross-role variable dependency pattern promoted (see below).

### kube-prometheus-stack: kube-prometheus-stack on k3s ARM64 via shell-based Helm
- **Avoid**: Three Helm `ansible.builtin.command` gotchas all caught at review, not lint: (1) `helm repo add` writes "already exists" to **stdout** not stderr — `changed_when` on `.stderr` produces a false positive on every run; (2) `creates: /usr/local/bin/helm` silently blocks re-running when the version variable changes — use a version-check `when` guard with `failed_when: false` (binary absent = command fails); (3) `changed_when: "has been deployed"` matches nothing in Helm 3 — correct strings are `"has been installed"` (first deploy) and `"has been upgraded"` (subsequent runs).
- **Repeat**: Researcher-first surfaced all 10 k3s-specific gotchas (5 disabled ServiceMonitors, hyphenated sub-chart key names, ARM64 admission webhook timeout, `serviceMonitorSelectorNilUsesHelmValues`, Grafana default dashboards) before a single task was written. None of these are in official kube-prometheus-stack docs — they are only discovered through k3s community sources and would all produce silent failures.
- **Updated**: `domains/raspberry-pi-hardware.md` — added "Helm on k3s (ARM64)" section covering version-pinned binary install pattern, stdout vs stderr for helm commands, correct `changed_when` strings, all required k3s overrides table, and temp values file security pattern.

### selenium-grid: Selenium Grid 4 KEDA job-autoscaling on ARM64 k3s
- **Avoid**: (1) `docker-selenium/selenium-grid` chart has no `chromiumNode` key — the only browser-node key is `chromeNode` regardless of browser type; ARM64 Chromium is selected via `chromeNode.imageName: node-chromium`. Helm silently ignores unknown keys, so `chromiumNode: enabled: true` deploys ZERO browser nodes with no error or warning. (2) Chart version and image tag are tightly coupled — chart 0.27.0 bundles image 4.17.0; image tag 4.43.0 belongs to chart 0.54.0. Mismatching causes hub/node protocol incompatibility that surfaces only at test execution time, not at deploy time. (3) Never hard-code deployment names in `kubectl rollout status` after Helm — chart naming helpers change across versions; use `ansible.builtin.uri` against the `/readyz` endpoint instead.
- **Repeat**: The coder → reviewer → fixer pipeline caught all 4 breaking bugs before any code reached the cluster. `ansible-lint --profile production` misses Helm values-key correctness entirely — a reviewer with chart knowledge is the only gate for this class of bug.
- **Updated**: Appended `docker-selenium/selenium-grid` ARM64 section to `.context/domains/raspberry-pi-hardware.md`.

### playbook-tags: Concern-based Ansible tags on stage.yaml
- **Avoid**: Stale uncommitted files on `main` (even trivial renames) block `git pull --rebase` and force an unplanned fix commit before the feature branch can be created. Run `git status` on `main` before creating any feature branch; clean up or stash loose changes first.
- **Repeat**: Presenting a complete role-to-tag mapping table in the @coder delegation brief resulted in a single-pass, zero-question implementation. For configuration tasks with a finite enumerable mapping (tags, labels, env vars), include the full table in the delegation — it eliminates every "which tag for this role?" decision the coder would otherwise ask.
- **Updated**: Created `.context/domains/ansible-playbook.md` — playbook structure, full tag map, common `--tags`/`--skip-tags` invocations, and role-ordering note.

### selenium-media: Screenshot + video storage for Selenium Grid on k3s
- **Avoid**: (1) Classic `filebrowser/filebrowser` (used by the chart's built-in `videoManager`) has **no WebDAV** — it is read-only browse only. The laptop cannot write screenshots to it. Use **FileBrowser Quantum** (`ghcr.io/gtsteffaniak/filebrowser`) as a separate Deployment for the WebDAV write target. (2) MinIO official ARM64 image was discontinued October 2025 — do not use; also requires S3 SDK code changes in the test framework which breaks the no-code-change constraint. (3) Longhorn RWX PVC requires `storageClassName: longhorn-rwx` with a custom StorageClass pointing at `driver.longhorn.io` — using the default `longhorn` StorageClass with `ReadWriteMany` will fail because `longhorn` is RWO only.
- **Repeat**: `selenium/video` FFmpeg image uses a **separate tag format** (`ffmpeg-8.1-20260404`) from hub/node images (`4.43.0-20260404`). Must set `global.seleniumGrid.videoImageTag` explicitly in Helm values or the chart may attempt to pull with the wrong tag. ARM64 FFmpeg is software-only — cap `SE_FRAME_RATE` at 10 (not default 30) to avoid saturating a Pi 4B core per recording session.
- **Updated**: Extended pre-push prerequisite section in `.context/domains/raspberry-pi-hardware.md` with video + filebrowser Quantum image commands; added "Selenium Media" section with NodePort table, PVC layout, macOS WebDAV mount instructions, and video tuning table.

### multi-board-support: NanoPC T-4 / RK3399 hardware gates in Ansible roles
- **Avoid**: (1) `ansible_board_name` and `ansible_product_name` are **always empty on ARM SBCs** — both Pi 4B and NanoPC T-4 return `""` because ARM boards don't expose DMI/SMBIOS. Never use these for board detection; use `/proc/device-tree/model` via `ansible.builtin.slurp` instead. (2) A role's `handlers/` directory can exist but contain no handlers — `notify: reboot` in `boot_opts` was silently no-op because `handlers/main.yaml` was empty. Always verify `handlers/main.yaml` is non-empty when a `notify:` is present. (3) Cross-role facts (like `board_platform`) intentionally don't follow the role-prefix naming convention — suppress `var-naming[no-role-prefix]` lint with `# noqa` and document why.
- **Repeat**: Researcher-first with actual role task file contents (not just role names) produced a precise per-role impact matrix: 20 of 24 roles unchanged, 4 needing gates, 1 already correct for NanoPC. Defensive hardware detection in `ssd_mount` (NVMe-first regex) meant zero changes needed there — design choices that detect dynamically rather than hardcode device paths avoid future board-porting work.
- **Updated**: Added "Multi-Board Ansible Detection" section to `raspberry-pi-hardware.md` (why ansible_board_name fails, device-tree pattern, board_platform gates); updated `ansible-playbook.md` tag table to include `board_detect` and added note that it must be first in both plays. Full NanoPC reference in `.context/domains/nanopc-t4-hardware.md`.
