- name: Swapoff
  ansible.builtin.command: swapoff -a
  changed_when: false

- name: Purge dphys-swapfile package
  ansible.builtin.apt:
    name: dphys-swapfile
    state: absent
    purge: true

- name: Remove /var/swap file
  ansible.builtin.file:
    path: /var/swap
    state: absent
