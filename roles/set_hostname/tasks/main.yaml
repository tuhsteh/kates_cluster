---
- name: Set hostname
  ansible.builtin.hostname:
    name: "{{ inventory_hostname }}"

- name: Check hostname
  ansible.builtin.command: hostname
  register: set_hostname_result
  changed_when: false

- name: Debug hostname
  ansible.builtin.debug:
    var: set_hostname_result.stdout
