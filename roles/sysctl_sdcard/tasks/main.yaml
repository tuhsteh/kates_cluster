- name: Deploy sysctl SD card wear reduction config
  ansible.builtin.copy:
    src: 99-sdcard.conf
    dest: /etc/sysctl.d/99-sdcard.conf
    owner: root
    group: root
    mode: '0644'
  notify: Apply sysctl
